# 🚀 Быстрый старт DevBlog

## 📍 Важно: Перейдите в директорию проекта!

Все скрипты нужно запускать из директории проекта:

```bash
cd ~/DevBlog
```

или

```bash
cd /Users/ainur/DevBlog
```

## 🎯 Запуск приложения

### Способ 1: Основной скрипт (рекомендуется)
```bash
cd ~/DevBlog
./start.sh
```

### Способ 2: Быстрый запуск
```bash
cd ~/DevBlog
./quick-start.sh
```

### Способ 3: Вручную (два терминала)

**Терминал 1 - Backend:**
```bash
cd ~/DevBlog/server
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd ~/DevBlog/client
npm run dev
```

## ✅ Проверка

После запуска откройте в браузере:
- **http://localhost:3000** (Frontend)
- **http://localhost:5000** (Backend API)

## 🔍 Если скрипты не работают

1. Убедитесь, что вы в правильной директории:
   ```bash
   pwd
   # Должно быть: /Users/ainur/DevBlog
   ```

2. Проверьте, что скрипты исполняемые:
   ```bash
   ls -la start.sh quick-start.sh
   ```

3. Если нужно, сделайте скрипты исполняемыми:
   ```bash
   chmod +x start.sh quick-start.sh
   ```

## 📝 Структура проекта

```
~/DevBlog/
├── start.sh          # Основной скрипт запуска
├── quick-start.sh     # Быстрый запуск
├── server/            # Backend
└── client/            # Frontend
```

