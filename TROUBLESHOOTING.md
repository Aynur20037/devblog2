# Решение проблем с запуском DevBlog

## Проблема: localhost:3000 не открывается в Safari

### Шаг 1: Проверьте, что сервер запущен

```bash
# Запустите приложение
./start.sh
```

Должны увидеть:
```
🚀 Сервер запущен на порту 5000
VITE v5.4.21  ready in XXX ms
➜  Local:   http://localhost:3000/
```

### Шаг 2: Проверьте порты

```bash
# Проверка порта 3000
lsof -ti:3000

# Проверка порта 5000
lsof -ti:5000
```

### Шаг 3: Попробуйте разные адреса в Safari

1. **http://localhost:3000**
2. **http://127.0.0.1:3000**
3. **http://0.0.0.0:3000** (может не работать)

### Шаг 4: Проверьте настройки Safari

1. Откройте **Настройки Safari** → **Конфиденциальность**
2. Убедитесь, что **"Предотвращать межсайтовое отслеживание"** не блокирует localhost
3. Попробуйте в режиме **Приватного просмотра**

### Шаг 5: Проверьте консоль разработчика

1. В Safari: **Разработка** → **Показать веб-инспектор**
2. Посмотрите ошибки в консоли
3. Проверьте вкладку **Сеть** - какие запросы не проходят

### Шаг 6: Альтернативный запуск

Если `./start.sh` не работает, запустите вручную:

```bash
# Терминал 1 - Backend
cd server
npm run dev

# Терминал 2 - Frontend  
cd client
npm run dev
```

### Шаг 7: Очистка и переустановка

```bash
# Остановите все процессы
pkill -f "node"
pkill -f "vite"

# Очистите кэш
cd client
rm -rf node_modules/.vite
rm -rf dist

# Переустановите зависимости (если нужно)
cd ..
npm run install-all
```

### Шаг 8: Проверка файрвола

```bash
# Проверьте, не блокирует ли файрвол порт 3000
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --listapps
```

## Быстрая проверка

Выполните эту команду для диагностики:

```bash
echo "=== Проверка портов ===" && \
lsof -ti:3000 && echo "✅ Порт 3000 занят" || echo "❌ Порт 3000 свободен" && \
lsof -ti:5000 && echo "✅ Порт 5000 занят" || echo "❌ Порт 5000 свободен" && \
echo "=== Проверка доступности ===" && \
curl -s http://localhost:3000 > /dev/null && echo "✅ localhost:3000 доступен" || echo "❌ localhost:3000 недоступен" && \
curl -s http://localhost:5000/api/articles > /dev/null && echo "✅ Backend API доступен" || echo "❌ Backend API недоступен"
```

## Если ничего не помогает

1. Попробуйте другой браузер (Chrome, Firefox)
2. Проверьте логи в терминале где запущен `./start.sh`
3. Убедитесь, что Node.js установлен: `node --version`
4. Проверьте версию npm: `npm --version`

